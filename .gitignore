# Byte-compiled / optimized
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.Python

# Virtual environments
.venv/
venv/
env/
ENV/

# Secrets & local config
# The *.template.ini files in config/ are the references to copy from.
config/bot.ini
config/tokens.ini
config.py

# Lavalink server (jar + config downloaded by ./setup-lavalink.sh)
lavalink/

# i18n: intermediate per-language translation JSON (assembled into the .po files)
locales/_work/

# Test artifacts
.pytest_cache/
.coverage
htmlcov/

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/
# Added by code-review-graph
.code-review-graph/
# Local, machine-specific MCP config (absolute venv path); do not commit
.mcp.json
# Local Claude Code guidance only - never committed/shared
CLAUDE.md
# Local Claude Code skills / config (workflow tooling, not bot code)
.claude/
